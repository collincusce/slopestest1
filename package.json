{
  "name": "t1",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "rm -rf ./dist && npx tsc -b",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "slopes": "^1.0.14"
  },
  "devDependencies": {
    "typescript": "^3.7.5"
  }
}
